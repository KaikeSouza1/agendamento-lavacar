/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'i.imgur.com', // Você já tinha este, vamos manter
        port: '',
        pathname: '/**',
      },
      // ADICIONE ESTE NOVO BLOCO PARA O CLOUDINARY
      {
        protocol: 'https',
        hostname: 'res.cloudinary.com',
        port: '',
        pathname: '/**',
      },
    ],
  },
}

module.exports = nextConfig